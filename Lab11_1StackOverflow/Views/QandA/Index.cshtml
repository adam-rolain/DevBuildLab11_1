@model List<Question>
@{
    ViewData["Title"] = "Index";
}

<h1>All Questions</h1>

@foreach (Question question in Model)
{
    <div>@question.Title</div>
    <div>@question.Detail</div>
    <div>@question.Posted</div>
}